version:             "3"

services:

    # Tests
    ${CNTR_TEST}:
        env_file:        ${ENV_FILE}
        build:
          context:       .
          dockerfile:    ${DOCKER_FILE_TEST}
        depends_on:
            - "${CNTR_NFVO}"
        # Mount current downloaded folder in another location
        volumes:
            - ${SRC_PATH}:${DST_PATH}
        command:         python3 ${DST_PATH}/test/main.py
