#
# MongoDB
#

FROM ubuntu:16.04

LABEL project="${CNTR_PROJECT}"

WORKDIR ${SHARED_FOLDER_DEV}/bin/docker

CMD ["mongod", "--port", "${DB_PORT}"]

ENTRYPOINT ["/bin/bash", "${DST_PATH}/bin/docker/setup-db.sh"]
